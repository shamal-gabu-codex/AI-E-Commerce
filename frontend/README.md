# Frontend

Next.js App Router frontend for AI E-Commerce Sales & Inventory Intelligence.

## Run

```bash
npm install
copy .env.local.example .env.local
npm run dev
```

Set `NEXT_PUBLIC_API_BASE_URL` to your FastAPI server. The default is `http://localhost:8000`.
